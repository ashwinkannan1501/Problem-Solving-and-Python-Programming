# Minimum element in a list

array = [1, 2, 89, 0, 200] # Define an list by using "[]"

print("Minimum number in the list is : ", min(array)) # Print the minimum element in a list by using "min()" function.
