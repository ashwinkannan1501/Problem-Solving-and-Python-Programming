# Python Program to get the current python version
# Import the 'sys'(system) module to get the current python version
import sys
print(f'Python Version : {sys.version}')
print(f'Version Information : {sys.version_info}')
