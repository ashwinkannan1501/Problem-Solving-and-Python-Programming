# Python program that will accept a base and height of a triangle and compute the area
base = float(input("Enter the base of a triangle : "))
height = float(input("Enter the height of the triangle : "))
area_of_a_triangle = (base*height)/2
print(f'The area of a triangle when base({base}) and height({height}) is : {area_of_a_triangle}')