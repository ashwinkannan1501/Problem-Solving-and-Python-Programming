# clear() function
"""The clear function is used to remove all the elements (key, value) pair in the dictionary
SYNTAX : dictionary.clear()"""

dictionary = {1: "one", 2: "two", 3: "three"}
print(f'Dictionary before using "clear()" function : {dictionary}')
dictionary.clear()
print(f'Dictionary after using "clear()" function : {dictionary}')
