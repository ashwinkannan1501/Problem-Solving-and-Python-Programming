# Python program to get the volume of a sphere with radius 6
# Import the 'math' module to do the mathematical operations
from math import pi, pow
radius = float(input("Enter the radius value : "))
print(f'The volume of a sphere when radius({radius}) is : {4.0/3.0*pi*pow(radius, 3)}')
