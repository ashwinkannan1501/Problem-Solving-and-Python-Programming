# Python program to get the 4th element from the first and the 4th element from the last of a tuple
tuple_elements = tuple(input("Enter the tuple integers : "))
print(f'The first 4th element from the tuple is : {tuple_elements[3]}')
print(f'The last 4th element from the tuple is : {tuple_elements[-4]}')
