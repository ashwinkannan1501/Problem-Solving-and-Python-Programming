# Python program to find the ASCII value of the character
character = input("Enter a single character to find the ASCII value : ")

# Using built-in "ord()" function to find the ASCII value (or) a integer value of a specific single character
print(f'The ASCII value of "{character}" is : {ord(character)}')